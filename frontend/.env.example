# Base URL of the deployed backend API (Render). Leave empty in local dev
# (the Vite dev server proxies /api to the backend). Example for prod:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
